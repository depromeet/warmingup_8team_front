export interface Props {
  bold?: boolean,
  borderRadius?: number,
  fontSize?: number,
  height?: number,
  width?: number,
  margin?: string,
  text: string,
  path?: string,
  handleClick?: () => void,
}

export interface Style {
  bold?: boolean,
  borderRadius?: number,
  fontSize?: number,
  height?: number,
  width?: number,
  margin?: string,
}
