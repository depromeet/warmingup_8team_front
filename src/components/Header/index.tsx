import React from 'react';
import * as styled from './style';

const Input: React.FC = () => {
  return (
    <styled.Header>
      <styled.HeaderText>
        KKIROOK CHAT
      </styled.HeaderText>
    </styled.Header>
  );
};

export default Input;
